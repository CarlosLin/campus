<%= simple_form_for @post do |f| %>
  <%= f.input :name, label: 'Post Title' %>
  <%= f.input :content, placeholder: 'fk'%>
  <%= f.button :submit, class: 'btn btn-default' %>
  <%= link_to "Cancel", :back, class: 'btn btn-default' %>
<% end %>
